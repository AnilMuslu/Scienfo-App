import 'package:flutter/material.dart';

class LikedContentField extends StatelessWidget {
  LikedContentField({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Stack(
      children: <Widget>[
        Container(
          width: 135.0,
          height: 199.0,
          color: const Color(0xfffdb25c),
        ),
      ],
    );
  }
}
